import { IS_INDEX, getById } from './constants.js'
import views from '../views/index.js'

export const createEvents = () => {
    if (IS_INDEX) {
        //index event
    } else {
        // global events
    }
}